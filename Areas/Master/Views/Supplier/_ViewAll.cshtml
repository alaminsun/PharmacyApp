@using PhramacyApp.Areas.Master.Models
@model IEnumerable<SupplierModel>
<table class="table table-striped table-bordered" id="customerTable">
    <thead>
        <tr>
            <th>
                @localizer["Id"]
            </th>
            <th>
                @localizer["SupplierId"]
            </th>
            <th>
                @localizer["Supplier Name"]
            </th>
            <th>
                @localizer["Email"]
            </th>
            <th>
                @localizer["Phone"]
            </th>
            <th>
                @localizer["Address"]
            </th>
            <th>
                @localizer["Notes"]
            </th>
            <th style="width:10%">
                @localizer["Actions"]
            </th>
        </tr>
    </thead>
    <tbody>

        @foreach (var supplier in Model)
        {
        <tr>
            <td>
                @supplier.Id
            </td>
            <td>
                @supplier.Supplier_Id
            </td>
            <td>
                @supplier.Supplier_Name
            </td>
            <td>
                @supplier.Email
            </td>
            <td>
                @supplier.Phone
            </td>
            <td>
                @supplier.Address
            </td>
            <td>
                @supplier.Notes
            </td>
            <td text-right">
                <div class="btn-group">
                    <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Action
                    </button>
                    <div class="dropdown-menu shadow animated--grow-in">
                        <a onclick="jQueryModalGet('/master/supplier/OnGetCreateOrEdit?id=@supplier.Id', 'Edit Supplier')" class="dropdown-item">
                            <i class="fas fa-pencil-alt"></i> @localizer["Edit"]
                        </a>
                        <form id="form_delete_@supplier.Id" asp-controller="supplier" method="post" asp-action="OnPostDelete" asp-route-id="@supplier.Id" onsubmit="return jQueryModalDelete(this)" class="d-inline">
                            <a href="javascript:$('#form_delete_@supplier.Id').submit();" class="dropdown-item"><i class="fas fa-trash-alt"></i> @localizer["Delete"]</a>
                        </form>
                    </div>
                </div>
            </td>
        </tr>
        }
    </tbody>
</table>
<script>
    $(document).ready(function () {
        $("#customerTable").DataTable();
    });
</script>
